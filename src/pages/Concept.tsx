import { useEffect } from "react"; import { useNavigate } from "react-router-dom"; import { motion } from "framer-motion"; import { Button } from "@/components/ui/Button"; import { PageLayout } from "@/components/layout/PageLayout"; import { useSurveyContext } from "@/hooks/useSurvey"; import { useAnalytics } from "@/hooks/useAnalytics";
export function Concept(){const {state,jumpToStage}=useSurveyContext();const {fire}=useAnalytics();const navigate=useNavigate();useEffect(()=>{if(state.stage!=="concept")navigate("/survey",{replace:true})},[state.stage,navigate]);if(state.stage!=="concept")return null;return <PageLayout className="justify-center"><div className="flex flex-1 flex-col justify-center py-16"><motion.div initial={{opacity:0,y:10}} animate={{opacity:1,y:0}} transition={{duration:.35}}><div className="mb-6 text-[11px] uppercase tracking-[0.28em] text-mooddly-muted">Interlude</div><h1 className="font-display text-[36px] font-semibold leading-[1.08] tracking-tight text-mooddly-white sm:text-[52px]">NOW IMAGINE THIS.</h1><p className="mt-8 max-w-lg text-[15px] leading-relaxed text-mooddly-white/75">A drink designed around what you actually need from your day.</p><ul className="mt-6 space-y-2 text-[15px] text-mooddly-white/85"><li>Hydration.</li><li>Function.</li><li>Refreshment.</li></ul><p className="mt-6 text-[15px] text-mooddly-white/60">Without unnecessary stuff.</p><div className="mt-10"><Button onClick={()=>{fire("concept_viewed");jumpToStage("survey")}}>WHAT DO YOU THINK? →</Button></div></motion.div></div></PageLayout>}
