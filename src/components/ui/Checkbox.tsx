import { cn } from "@/lib/utils";
type Props={checked:boolean;onChange:(v:boolean)=>void;label:string};
export function Checkbox({checked,onChange,label}:Props){return <button type="button" role="checkbox" aria-checked={checked} onClick={()=>onChange(!checked)} className="flex w-full items-start gap-4 text-left"><span aria-hidden="true" className={cn("mt-0.5 flex h-5 w-5 shrink-0 items-center justify-center rounded border transition-colors",checked?"border-mooddly-cyan bg-mooddly-cyan":"border-mooddly-border")}>{checked&&<svg viewBox="0 0 16 16" className="h-3 w-3 text-mooddly-bg" fill="none" stroke="currentColor" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"><path d="M3 8l3 3 7-7"/></svg>}</span><span className="text-[14px] leading-relaxed text-mooddly-white/90">{label}</span></button>}
